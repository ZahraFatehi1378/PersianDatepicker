<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/timePicker">
    <TimePicker
        android:id="@+id/mySimpleTimePicker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:timePickerMode="spinner"
        tools:ignore="MissingConstraints" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/save"
        android:id="@+id/saveTime"
        app:layout_constraintEnd_toEndOf="@+id/mySimpleTimePicker"
        app:layout_constraintStart_toStartOf="@+id/mySimpleTimePicker"
        app:layout_constraintTop_toBottomOf="@id/mySimpleTimePicker" />
</androidx.constraintlayout.widget.ConstraintLayout>